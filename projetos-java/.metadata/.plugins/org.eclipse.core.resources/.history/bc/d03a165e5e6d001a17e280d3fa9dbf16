function validarCampos() {
	var peso = document.getElementByName('peso');
	var altura = document.getElementByName('altura');
	
	if(!isNaN(peso) || !isNaN(altura)) {
        alert('Valor inválido!');
		return false;
	}
}